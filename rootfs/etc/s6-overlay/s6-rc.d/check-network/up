#!/bin/bash
echo "[Network Check] 正在检查网络连通性..."
# 建议加上 -m 5 防止 curl 卡死，使用 http_code 判断更稳健
if curl -I -s -o /dev/null -w "%{http_code}" http://www.baidu.com | grep "200" > /dev/null; then
    echo "[Network Check] 网络连接正常！"
else
    echo "[Network Check] 警告：无法连接外网，但 Nginx 将继续尝试启动。"
fi
exit 0